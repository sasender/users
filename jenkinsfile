@Library('todoshop') _

todoshop (
        COMPONENT             : 'user',
        PROJECT_NAME          : "todoshop",
        SLAVE_LABEL           : "NODEJS",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "JAVA"
)
